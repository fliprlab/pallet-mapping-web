import { userApiPrefix } from "../../constants";

export const putAwayApis = {
  GRID_CHECK_VALID_PALLET: userApiPrefix + "/grid-check-valid-pallet",
  MAPPED_GRID: userApiPrefix + "/mapped-grid",
};
